specVersion: "v1"
serviceId: "smartmess"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "smartmess-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
